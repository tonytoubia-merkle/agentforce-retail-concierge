{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "src": "/api/generate-journey-image", "dest": "/api/generate-journey-image" },
    { "src": "/api/(.*)", "dest": "/api/proxy" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "functions": {
    "api/proxy.js": {
      "maxDuration": 120
    },
    "api/generate-journey-image.js": {
      "maxDuration": 120
    }
  }
}
